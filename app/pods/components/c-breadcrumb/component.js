import Ember from 'ember';

export default Ember.Component.extend({
  // Component setup
  classNames: ['c-breadcrumb'],
  bemBlockName: 'c-breadcrumb',
  tagName: 'div',

  // Handed in properties
  items: Ember.A()

  // Closures

  // Internal properties

  // Internal functions

  // Actions
});
