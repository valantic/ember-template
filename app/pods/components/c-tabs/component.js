import Ember from 'ember';

export default Ember.Component.extend({
  // Component setup
  classNames: ['c-tabs'],
  bemBlockName: 'c-tabs',
  tagName: 'div',

  // Handed in properties
  items: Ember.A(),
  componentName: false,
  data: null

  // Closures

  // Internal properties

  // Internal functions

  // Actions
});
