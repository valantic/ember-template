{{! template-lint-disable }}
<!-- disable all rules -->
{{#freestyle-subsection name='c-media-container' section=section}}

  {{#freestyle-annotation}}
    <p>The <code>c-media-container</code> Component is used for rendering a seperate image for each viewport</p>
    <h4>Attributes</h4>
    <dl>
      <dt><code>medias</code> (Array)</dt>
      <dd>Array with media objects object => {altText, url, mediaFormat}</dd>
      <dt><code>selectorXXS</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
      <dt><code>selectorXS</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
      <dt><code>selectorSM</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
      <dt><code>selectorMD</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
      <dt><code>selectorLG</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
      <dt><code>selectorXL</code> (String)</dt>
      <dd>the string that is used in the medias array as "mediaFormat"</dd>
    </dl>
  {{/freestyle-annotation}}

  {{#freestyle-collection title='c-media-container' defaultKey='all' inline=false as |collection|}}

    {{#freestyle-variant collection=collection key='default'}}
      {{#freestyle-usage 'c-media-container-default' title='c-media-container default' usageTitle=labels.usage.title}}

        {{c-media-container
          medias=model.mockdata.cMediaContainer.medias
          selectorXXS="imageXXS"
          selectorXS="imageXS"
          selectorSM="imageSM"
          selectorMD="imageMD"
          selectorLG="imageLG"
          selectorXL="imageXL"
        }}

      {{/freestyle-usage}}
    {{/freestyle-variant}}


  {{/freestyle-collection}}
{{/freestyle-subsection}}
